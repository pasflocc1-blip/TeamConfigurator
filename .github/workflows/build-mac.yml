name: Build Mac App

on:
  push:
    tags:
      - 'v*'          # Attivato da tag tipo: v1.0.0, v1.2.3
  workflow_dispatch:   # Oppure manualmente dalla UI GitHub

    
jobs:
  build-mac:
    runs-on: macos-latest   # Runner Mac gratuito su GitHub Actions
    permissions:
        contents: write
    env:
        FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        
    steps:
      # 1. Checkout del codice
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Setup Python
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3. Setup Node.js per il build Vue3
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # 4. Build frontend Vue3
      - name: Build Frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build
        # Output va in backend/frontend_dist (vedi vite.config.js)

      # 5. Installa dipendenze Python + PyInstaller
      - name: Install Python deps
        working-directory: backend
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      # 6. Build .app con PyInstaller
      - name: Build .app con PyInstaller
        working-directory: backend
        run: |
          ls -la
          pyinstaller myapp.spec --noconfirm
          ls -la dist/

      # 7. Crea archivio ZIP della .app
      - name: Zip .app
        run: |
          cd backend/dist
          ls -la
          zip -r MyApp-mac.zip .

      # 8. Carica come artifact scaricabile
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: MyApp-mac
          path: backend/dist/MyApp-mac.zip
          retention-days: 30

      # 9. Se è un tag, crea anche una Release GitHub
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: backend/dist/MyApp-mac.zip
          generate_release_notes: true